source 'https://rubygems.org'


# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '~> 5.0.0', '>= 5.0.0.1'
# Use postgresql as the database for Active Record
gem 'pg', '~> 0.18.4'
# Use Puma as the app server
gem 'puma', '~> 3.0'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.2'
# See https://github.com/rails/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby
# Simple image helpers for the rails project using react
gem 'react-rails-img', '~> 0.1.5'
# Use jquery as the JavaScript library
gem 'jquery-rails'
# Turbolinks makes navigating your web application faster. Read more: https://github.com/turbolinks/turbolinks
gem 'turbolinks', '~> 5'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.5'
# Browserify Support
gem 'browserify-rails', '~> 3.1'
# Babel support
gem 'babel-transpiler', '~> 0.7.0'
# Reactjs and JSX support.
gem 'react-rails', '~> 1.8', '>= 1.8.2'
# Use Redis adapter to run Action Cable in production
gem 'redis', '~> 3.0'
# Use ActiveModel has_secure_password
gem 'bcrypt', '~> 3.1.7'
# Flexible authentication solution for Rails with Warden
gem 'devise', '~> 4.2'
# Gravatar helpers
gem 'gravatarify', '~> 3.1', '>= 3.1.1'
# Filterrific is a Rails Engine plugin that makes it easy to filter, search, and sort your ActiveRecord lists.
gem 'filterrific', '~> 2.0', '>= 2.0.5'
# Provides a simple API for performing paginated queries
gem 'will_paginate', '~> 3.1', '>= 3.1.3'
# Easy upload management for ActiveRecord
gem 'paperclip', '~> 5.1'
# Soft-deletion support gem
gem 'paranoia', '~> 2.2.0.pre'
# Flexible rack authentication framework
gem 'omniauth', '~> 1.3', '>= 1.3.1'
# Json Token-based devise auth
gem 'devise_token_auth', '~> 0.1.39'
# All sorts of useful information about every country packaged as pretty little country objects
gem 'countries', '~> 1.2', '>= 1.2.5'

group :development do
  # Access an IRB console on exception pages or by using <%= console %> anywhere in the code.
  gem 'web-console'
  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'
  gem 'spring-watcher-listen', '~> 2.0.0'
  # Use Capistrano for deployment
  gem 'capistrano', '~> 3.6', '>= 3.6.1'
  gem 'capistrano-rails', '~> 1.2'
  # Bundler support for Capistrano 3.x
  gem 'capistrano-bundler', '~> 1.2'
  # Provide deployment tasks for puma
  gem 'capistrano3-puma', '~> 1.2', '>= 1.2.1'
  # npm support
  gem 'capistrano-npm', '~> 1.0', '>= 1.0.2'
  # Bower support
  gem 'capistrano-bower', '~> 1.1'
  # rbenv support
  gem 'capistrano-rbenv', '~> 2.0', '>= 2.0.4'
end

group :test do
  # WebMock allows stubbing HTTP requests and setting expectations on HTTP requests.
  gem 'webmock', '~> 2.1'
end

group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug', platform: :mri
  # Testing framework
  gem 'rspec-rails', '~> 3.5', '>= 3.5.2'
  # Framework and DSL for defining and using factories
  gem 'factory_girl_rails', '~> 4.7'
  # Lets helps you test web applications by simulating how a real user would interact with your app
  gem 'capybara', '~> 2.10', '>= 2.10.1'
  # Guard::RSpec automatically run your specs
  gem 'guard-rspec', '~> 4.7', '>= 4.7.3'
  # Rspec command for spring
  gem 'spring-commands-rspec', '~> 1.0', '>= 1.0.4'
  # Record your test suite's HTTP interactions and replay them during future test runs for fast, deterministic, accurate tests.
  gem 'vcr', '~> 3.0', '>= 3.0.3'
end

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]
# This should not be here, but as a workaround it has been placed here. Please remove when bugfix comes out. #https://github.com/rails/rails/issues/24063
gem 'listen', '~> 3.0.5'
